version: 2
notify: 
   webhooks:
     - url: https://discordapp.com/api/webhooks/294669249472102411/P5dEmGywuvDB0oIihZD0SVOSk-r_vrBAPSU-CfsEF2qsGjHb0xxVmQgE_U5nmTn8nP9s/circle
jobs:
  build:
    docker:
      - image: node:7.4.0
    working_directory: src
    steps:
      - setup_remote_docker
      - checkout
      - run:
          name: Install Docker client
          command: |
            set -x
            VER="17.03.0-ce"
            curl -L -o /tmp/docker-$VER.tgz https://get.docker.com/builds/Linux/x86_64/docker-$VER.tgz
            tar -xz -C /tmp -f /tmp/docker-$VER.tgz
            mv /tmp/docker/* /usr/bin
      - run:
          name: Install Dependencies
          command: npm i -S
      - run:
          name: Docker Build
          command: docker build -f Dockerfile -t clara:continous .